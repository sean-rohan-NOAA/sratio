% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gearcalib_nb.R
\name{gearcalib_plot}
\alias{gearcalib_plot}
\title{Plot log-Gaussian Cox process model and bootstrap results}
\usage{
gearcalib_plot(
  fit,
  select = c("relsel", "density"),
  boot = NULL,
  xlab = "Length group",
  add_bootquantiles = FALSE,
  log_transform_cpue = FALSE
)
}
\arguments{
\item{fit}{An object of class \code{"gearcalibFit"} as returned by \code{\link{gearcalib_fit}}.}

\item{select}{Character vector specifying which plots to return. Options are \code{"relsel"} (relative selectivity) and \code{"density"} (CPUE by gear). Default is \code{c("relsel", "density")}.}

\item{boot}{Optional list of bootstrap results from \code{\link{gearcalib_boot}}. Required for plotting observed ratios and densities.}

\item{xlab}{Character; label for the x-axis (default is \code{"Length group"}).}

\item{add_bootquantiles}{Logical; if \code{TRUE}, overlay bootstrap quantiles on the relative selectivity plot. Default is \code{FALSE}.}

\item{log_transform_cpue}{Logical; if \code{TRUE}, log-transform CPUE values (base 10) in the density plot. Default is \code{FALSE}.}
}
\value{
A named list of \code{ggplot} objects. Contains:
  \item{p_fit}{Relative selectivity plot (if \code{"relsel"} is selected).}
  \item{p_cpue}{CPUE by gear plot (if \code{"density"} is selected and \code{boot} is provided).}
}
\description{
Produces plots of relative selectivity ratios, sample sizes, and bootstrap density estimates by gear.
}
\details{
The relative selectivity plot shows model-based estimates (with ±2 SD ribbons) and optionally raw ratios and bootstrap quantiles.
The CPUE plot shows catch densities per swept area for both gears, with optional log-scale.
}
