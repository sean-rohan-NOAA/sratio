% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_ratio_estimator_bootstrap.R
\name{log_ratio_estimator_bootstrap}
\alias{log_ratio_estimator_bootstrap}
\title{Log ratio estimator with bootstrap confidence intervals}
\usage{
log_ratio_estimator_bootstrap(
  x,
  y,
  conf.level = 0.95,
  n_boot = 1000,
  return_boot = FALSE
)
}
\arguments{
\item{x}{A numeric vector of positive values (e.g., treatment group).}

\item{y}{A numeric vector of positive values (e.g., control group). Must be the same length as `x`.}

\item{conf.level}{Confidence level for the bootstrap confidence interval. Defaults to 0.95.}

\item{n_boot}{Number of bootstrap samples to draw. Defaults to 1000.}

\item{return_boot}{Logical; if `TRUE`, the function also returns the full bootstrap distribution (on the ratio scale). Defaults to `FALSE`.}
}
\value{
A list with the following components:
\describe{
  \item{mu}{Bias-corrected estimate of the geometric mean ratio.}
  \item{lci_mu}{Lower bound of the bootstrap confidence interval.}
  \item{uci_mu}{Upper bound of the bootstrap confidence interval.}
  \item{log_mu}{Mean of the log-ratios (i.e., log(x) - log(y)).}
  \item{n}{Sample size.}
  \item{bootstrap_distribution}{(Optional) Matrix of bootstrap estimates on the ratio scale, returned if `return_boot = TRUE`.}
}
}
\description{
Estimates the geometric mean of the log-ratio of two paired samples with 
a bias correction and computes a bootstrap confidence interval.
}
